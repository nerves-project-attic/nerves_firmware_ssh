use Mix.Config

# config :nerves_firmware_ssh,
#   authorized_keys: [
#     "ssh-rsa 12345== abcd",
#     "ssh-rsa 67890== fghi",
#   ],
#   system_dir: "/etc/ssh"

# The keys located in /etc/ssh should be named
#  ssh_host_rsa_key
#  ssh_host_rsa_key.pub
